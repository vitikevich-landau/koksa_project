pip install django-ckeditor
pip install mysqlclient
pip install Pillow
pip install transliterate

После клонирования с github'a
    - install virtual env with inherit packages
    - pip install django
